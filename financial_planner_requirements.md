# Финансовый планировщик - Техническое задание

## Версия документа: 1.0
**Дата:** 08 ноября 2025  
**Статус:** Финальная версия для MVP

---

## 1. Краткое описание проекта

### 1.1. Концепция

**Финансовый планировщик** — веб-приложение для среднесрочного и долгосрочного финансового планирования с фокусом на визуализацию траектории капитала и прогнозирование влияния крупных финансовых событий.

**Ключевое отличие от конкурентов:**  
Отказ от микроменеджмента ежедневных транзакций в пользу планирования по крупным регулярным и единовременным финансовым событиям.

**Аналогия:** Google Calendar для денег, а не детальный таймтрекер расходов.

### 1.2. Целевая аудитория

- **Молодые профессионалы** (25-40 лет) с относительно стабильным доходом
- **Семьи**, планирующие крупные покупки (недвижимость, автомобиль)
- **Фрилансеры** с нерегулярными доходами, нуждающиеся в визуализации cash flow
- Люди, планирующие **финансовую независимость** (FIRE движение)

**Общие черты:**
- Не хотят тратить время на учет каждой покупки кофе
- Нуждаются в понимании своей финансовой ситуации на горизонте 1-2 года
- Принимают решения о крупных покупках на основе прогноза

### 1.3. Основные сценарии использования

#### Сценарий 1: Планирование крупной покупки
```
Пользователь хочет купить автомобиль за $25,000 в августе 2026.

Действия:
1. Добавляет единовременное событие "Покупка авто" на 15.08.2026
2. Видит на графике прогноза, что баланс уйдет в минус в сентябре
3. Откладывает покупку на ноябрь 2026
4. График показывает, что теперь баланс остается положительным
5. Принимает решение о покупке
```

#### Сценарий 2: Изменение регулярного дохода
```
Пользователь получил повышение зарплаты с января 2026.

Действия:
1. Открывает recurring операцию "Зарплата"
2. Выбирает "Изменить с этого момента и далее"
3. Указывает новую сумму $6,500 (было $5,000)
4. График пересчитывается и показывает дополнительные $18,000 через год
5. Решает увеличить ежемесячные инвестиции с $1,000 до $1,500
```

#### Сценарий 3: Синхронизация с реальностью
```
Раз в 2-3 месяца пользователь проверяет соответствие прогноза реальности.

Действия:
1. Открывает банковское приложение, видит баланс $12,680
2. В финансовом планировщике прогноз показывает $12,450
3. Делает reconciliation: указывает реальный баланс
4. Система создает корректировочную запись на разницу $230
5. График обновляется с учетом новой точки отсчета
```

### 1.4. Ключевые метрики успеха MVP

- **Time to first forecast:** < 10 минут (от регистрации до первого графика)
- **Weekly active users retention:** > 40% через месяц использования
- **Average session duration:** 5-7 минут (быстрый просмотр + планирование)
- **Feature adoption:**
  - Создание recurring операций: > 80% пользователей
  - Использование reconciliation: > 60% пользователей
  - Добавление единовременных событий: > 70% пользователей

---

## 2. Технологический стек

### 2.1. Backend
- **Framework:** FastAPI 0.104+
- **Language:** Python 3.12+
- **ORM:** SQLAlchemy 2.0 (async)
- **Migrations:** Alembic
- **Authentication:** JWT (python-jose)
- **Password hashing:** bcrypt (passlib)
- **Validation:** Pydantic v2
- **Package manager:** uv

### 2.2. Frontend
- **Framework:** Angular 17+
- **UI Library:** ng-zorro-antd (Ant Design)
- **Charts:** Apache ECharts (через ng-zorro)
- **State Management:** RxJS + Angular Services (NgRx отложен на v1.1)
- **HTTP Client:** HttpClient (Angular)

### 2.3. База данных
- **Production:** PostgreSQL 16
- **Development:** SQLite (для локальной разработки)

### 2.4. Infrastructure
- **Containerization:** Docker + Docker Compose
- **Deployment:** Railway / Render / Fly.io (выбор на этапе деплоя)
- **Environment management:** .env файлы

### 2.5. Development Tools
- **Backend:**
  - pytest + pytest-asyncio (тестирование)
  - black + ruff (форматирование и линтинг)
  - pre-commit (git hooks)
  
- **Frontend:**
  - Prettier + ESLint
  - Husky (git hooks)
  
- **Database:**
  - DBeaver / pgAdmin (GUI)

---

## 3. Функциональные требования MVP

### 3.1. Аутентификация и безопасность

#### 3.1.1. Регистрация
- Регистрация по `email` и `password`
- Валидация email (формат)
- Требования к паролю:
  - Минимум 8 символов
  - Минимум 1 заглавная буква
  - Минимум 1 цифра
- Хранение паролей: bcrypt хеш
- **Без email верификации в MVP** (добавить в v1.1)

#### 3.1.2. Вход в систему
- Аутентификация по `email` и `password`
- Выдача JWT access token (срок жизни: 15 минут)
- Выдача JWT refresh token (срок жизни: 7 дней)
- Хранение refresh token в HTTPOnly cookie

#### 3.1.3. Управление сессией
- Эндпоинт для обновления access token через refresh token
- Автоматический logout при истечении refresh token
- Возможность выйти со всех устройств (инвалидация всех refresh tokens)

#### 3.1.4. Безопасность
- CORS настройки для production
- Rate limiting на эндпоинты аутентификации (макс. 5 попыток входа в минуту)
- Защита от CSRF через SameSite cookie атрибут

### 3.2. Главная страница (Dashboard)

#### 3.2.1. График прогноза баланса

**Описание:**  
Stacked area chart с 4-мя областями, показывающий прогноз финансового состояния.

**Компоненты графика:**
1. **Ликвидные средства** (синий)
   - Сумма всех счетов типа: CHECKING, SAVINGS, CASH
   - Отображается выше нулевой линии
   
2. **Инвестиции** (зелёный)
   - Сумма всех счетов типа: INVESTMENT, RETIREMENT
   - Отображается выше ликвидных средств (stacked)
   
3. **Использованный кредит** (красный)
   - Сумма отрицательных балансов кредитных счетов
   - Отображается ниже нулевой линии
   
4. **Долги к получению** (оранжевый) — опционально
   - Сумма счетов типа: LOAN_GIVEN
   - Отображается выше инвестиций

**Период отображения:**
- По умолчанию: от -6 месяцев до +24 месяцев от текущей даты
- Настраивается через фильтры: от -12 до +36 месяцев

**Интерактивность:**
- Zoom (колесо мыши / pinch на тачскрине)
- Tooltip при наведении на точку (показывает все 4 значения на конкретную дату)
- Вертикальная линия "Сегодня" для ориентации

**Конвертация валют:**
- Все значения конвертируются в основную валюту пользователя
- Курс на каждую дату берется из кеша (для истории) или текущий (для прогноза)

#### 3.2.2. Карточки сводной информации

Четыре карточки над или под графиком с текущими значениями:

1. **💰 Ликвидные средства**
   - Формула: `SUM(balance) WHERE account.type IN (CHECKING, SAVINGS, CASH)`
   - Пример: `$12,450`
   - Субтекст: "Доступно сейчас"

2. **📈 Инвестиции**
   - Формула: `SUM(balance) WHERE account.type IN (INVESTMENT, RETIREMENT)`
   - Пример: `$35,000`
   - Субтекст: "Долгосрочные накопления"

3. **💳 Кредит**
   - Формула верхняя строка: `SUM(ABS(balance)) WHERE account.type = CREDIT_CARD AND balance < 0`
   - Формула нижняя строка: `SUM(credit_limit) WHERE account.type = CREDIT_CARD`
   - Пример: `$1,500 / $5,000`
   - Субтекст: "Использовано / Доступно"

4. **🤝 Долги вам** (опционально, можно скрыть если нет таких счетов)
   - Формула: `SUM(balance) WHERE account.type = LOAN_GIVEN`
   - Пример: `$500`
   - Субтекст: "Должны вернуть"

**Дополнительная метрика:**
- **Чистый капитал (Net Worth):**  
  `Ликвидные + Инвестиции - |Использованный кредит| + Долги вам`
  - Отображается крупным шрифтом в заголовке или отдельной карточкой
  - Пример: `$46,450`

#### 3.2.3. Список ближайших событий

**Описание:**  
Таблица или список следующих 10 запланированных операций (recurring + единовременные).

**Столбцы:**
- Дата (например, "25 ноя")
- Иконка категории
- Название операции
- Сумма (с цветом: зелёный для доходов, красный для расходов)
- Действия: [Редактировать] [Отложить]

**Функционал "Отложить":**
- Для единовременных событий: перенести на другую дату
- Для recurring событий: создать exception (пропустить это вхождение)

**Сортировка:** по дате (от ближайших к дальним)

#### 3.2.4. Блок "Требуется внимание"

Отображается, если выполняется одно из условий:

1. **Баланс счета отличается от прогноза более чем на 5% или $100**
   ```
   ⚠️ Баланс карты "Сбер" отличается от прогноза на $230
   [Сделать корректировку]
   ```

2. **Не было reconciliation более 60 дней**
   ```
   ⚠️ Последняя корректировка баланса была 3 месяца назад
   [Проверить счета]
   ```

3. **Прогноз показывает отрицательный баланс в ближайшие 3 месяца**
   ```
   ⚠️ Прогноз показывает недостаток средств в феврале 2026
   [Скорректировать планы]
   ```

### 3.3. Модуль "Счета" (Accounts)

#### 3.3.1. Типы счетов

```python
class AccountType(str, Enum):
    # Ликвидные средства
    CHECKING = "checking"           # Дебетовая карта
    SAVINGS = "savings"             # Накопительный счет
    CASH = "cash"                   # Наличные
    
    # Долгосрочные инвестиции  
    INVESTMENT = "investment"       # Брокерский счет, ИИС
    RETIREMENT = "retirement"       # Пенсионные накопления
    
    # Кредитные средства
    CREDIT_CARD = "credit_card"     # Кредитная карта
    LOAN = "loan"                   # Потребительский кредит
    
    # Долги к получению
    LOAN_GIVEN = "loan_given"       # Одолжили кому-то
```

#### 3.3.2. CRUD операции

**Создание счета:**
- Название (обязательно)
- Тип счета (обязательно, из enum)
- Валюта (обязательно, выбор из списка)
- Начальный баланс (обязательно)
- Кредитный лимит (только для CREDIT_CARD и LOAN)
- Дата создания счета (по умолчанию = сегодня)

**Просмотр списка счетов:**
- Группировка по типам (Ликвидные / Инвестиции / Кредиты / Долги)
- Для каждого счета:
  - Название
  - Текущий баланс (рассчитывается из initial_balance + все операции)
  - Валюта
  - Для кредитных карт: использовано / лимит

**Редактирование счета:**
- Изменение названия
- Изменение кредитного лимита
- **Нельзя изменить:** тип счета, валюту, начальный баланс

**Удаление счета:**
- Проверка: есть ли запланированные операции на этот счет?
- Если да → предупреждение + предложение перенести операции на другой счет
- Soft delete (помечается как удаленный, но остается в БД)

#### 3.3.3. Корректировка баланса (Reconciliation)

**Назначение:**  
Синхронизация прогноза с реальным состоянием счета.

**UI Flow:**
1. Пользователь открывает счет
2. Видит два значения:
   - **Прогноз:** $12,450 (рассчитано приложением)
   - **Реальный баланс:** [Ввести вручную]
3. Вводит реальный баланс из банковского приложения: $12,680
4. Нажимает "Скорректировать"
5. Система:
   - Создает корректировочную запись (adjustment transaction)
   - Пересчитывает прогноз от этой даты
   - Сохраняет reconciliation запись для истории

**Поля формы reconciliation:**
- Дата корректировки (по умолчанию = сегодня)
- Реальный баланс (обязательно)
- Примечание (опционально, например: "Забыл внести покупку ноутбука")

**История reconciliation:**
- Таблица всех корректировок по счету
- Столбцы: Дата | Ожидалось | Фактически | Разница | Примечание

### 3.4. Модуль "Планировщик операций" (Scheduler)

#### 3.4.1. Типы операций

```python
class TransactionType(str, Enum):
    INCOME = "income"       # Доход
    EXPENSE = "expense"     # Расход
    TRANSFER = "transfer"   # Перевод между счетами
```

#### 3.4.2. Создание операции

**Общие поля для всех типов:**
- Название (обязательно, например: "Зарплата", "Аренда", "Покупка MacBook")
- Тип операции (обязательно: доход / расход / перевод)
- Сумма (обязательно)
- Категория (обязательно, выбор из списка)
- Примечание (опционально)

**Для типа INCOME и EXPENSE:**
- Счет (обязательно)

**Для типа TRANSFER:**
- Со счета (обязательно)
- На счет (обязательно)
- Примечание: сумма списания = сумма зачисления (без учета конвертации валют в MVP)

**Выбор режима повторения:**

1. **⚪ Разовое событие**
   - Дата события (обязательно)

2. **⚫ Повторяющееся событие**
   - Частота: [Ежемесячно ▼ | Ежегодно]
   
   **Для "Ежемесячно":**
   - День месяца: [1-31 ▼] или "Последний день месяца"
   - Примеры:
     - Зарплата каждое 25 число → day = 25
     - Аренда каждое 1 число → day = 1
   
   **Для "Ежегодно":**
   - День: [1-31 ▼]
   - Месяц: [Январь ▼ ... Декабрь]
   - Пример:
     - Страховка автомобиля каждое 15 марта → day = 15, month = 3
   
   - Дата начала (обязательно, по умолчанию = сегодня)
   - Дата окончания (опционально):
     - ⚪ Бессрочно (по умолчанию)
     - ⚫ До даты: [выбор даты]

**Категории (системные, для MVP):**

Доходы:
- Зарплата
- Фриланс
- Инвестиционный доход
- Продажа имущества
- Прочий доход

Расходы:
- Базовые расходы (еда, транспорт, развлечения — всё вместе)
- Аренда / ипотека
- Коммунальные услуги
- Страхование
- Образование
- Здоровье
- Крупная покупка
- Прочие расходы

Переводы:
- В инвестиции
- Между счетами
- Погашение кредита

#### 3.4.3. Просмотр запланированных операций

**Два режима отображения:**

1. **Список recurring правил**
   - Группировка: Доходы | Расходы | Переводы
   - Для каждой операции:
     - Название
     - Сумма
     - Периодичность (например: "Каждое 25 число")
     - Счет
     - Действия: [Редактировать] [Удалить]

2. **Календарь развернутых событий**
   - Выбор периода: текущий месяц, 3 месяца, 6 месяцев, год
   - Показ конкретных дат операций с возможностью редактирования
   - Фильтр по типу: все | доходы | расходы | переводы

#### 3.4.4. Редактирование recurring операций

**Модальное окно с выбором режима:**

```
Это повторяющаяся операция. Что изменить?

⚪ Только эту операцию (создать исключение)
⚫ Все операции в серии
⚪ Эту и все будущие операции

[Отмена] [Продолжить]
```

**Логика режимов:**

1. **"Только эту операцию":**
   - Создается `ScheduledTransactionException` на конкретную дату
   - Пользователь может изменить сумму или отметить как "пропустить"
   - Пример: "В декабре зарплата с бонусом +$2,000"

2. **"Все операции в серии":**
   - Обновляется `ScheduledTransaction` запись
   - Изменения применяются ко всем прошлым и будущим экземплярам
   - Пример: "Категория была неправильной, исправить везде"

3. **"Эту и все будущие операции":** (сложнее, но важно для MVP)
   - Завершается текущая `ScheduledTransaction` (устанавливается `end_date` = день перед редактируемым событием)
   - Создается новая `ScheduledTransaction` с новыми параметрами (start_date = дата редактируемого события)
   - Пример: "С января зарплата увеличилась с $5,000 до $6,500"

#### 3.4.5. Удаление операций

**Для recurring операций:**
- Аналогичный выбор: только эту | всю серию | эту и будущие
- "Только эту" → создать exception с флагом `is_deleted = true`
- "Всю серию" → soft delete `ScheduledTransaction`
- "Эту и будущие" → установить `end_date` на предыдущий день

**Для разовых операций:**
- Простое удаление (soft delete)

### 3.5. Модуль "Анализ" — Упрощенная версия для MVP

**Назначение:**  
Сравнение плана с фактом, а не детальная аналитика трат.

#### 3.5.1. Основные метрики

**За выбранный период (месяц / 3 месяца / год):**

1. **План vs Факт по доходам:**
   ```
   Планировалось: $15,000
   Фактически:   $15,800  (+5%)
   ```

2. **План vs Факт по расходам:**
   ```
   Планировалось: $7,500
   Фактически:   $8,200   (+9%)
   ```

3. **План vs Факт по накоплениям:**
   ```
   Планировалось: $7,500
   Фактически:   $7,600   (+1%)
   ```

**Источник данных "Факт":**
- Берется из reconciliation записей
- Между reconciliation датами делается интерполяция

#### 3.5.2. График План vs Факт

**Line chart с двумя линиями:**
- Синяя сплошная: Фактический баланс (по reconciliation точкам)
- Серая пунктирная: Плановый баланс (по scheduled операциям)

**Период:** последние 6-12 месяцев (выбирается фильтром)

#### 3.5.3. Разбивка по крупным категориям

**Bar chart или таблица:**
- Категории (только крупные: Зарплата, Базовые расходы, Аренда, Инвестиции)
- Для каждой категории:
  - План
  - Факт (если есть reconciliation данные)
  - Отклонение (%)

**Не делаем в MVP:**
- Детальный анализ подкатегорий
- Круговые диаграммы "куда ушли деньги"
- Тренды и прогнозирование на основе истории

#### 3.5.4. Рекомендации

На основе анализа отклонений показывать простые подсказки:

```
💡 Ваши базовые расходы за последние 3 месяца в среднем на 12% выше плана.
   Рекомендуем увеличить планируемую сумму с $2,500 до $2,800/месяц.
   [Обновить план]
```

### 3.6. Настройки пользователя

#### 3.6.1. Профиль
- Email (отображается, нельзя изменить в MVP)
- Имя (опционально)
- Основная валюта (обязательно, выбор из списка)
  - При изменении: пересчитываются все отчеты
  - Не влияет на валюты счетов (они остаются в оригинальных валютах)

#### 3.6.2. Безопасность
- **Смена пароля:**
  - Текущий пароль (для подтверждения)
  - Новый пароль (с валидацией)
  - Подтверждение нового пароля
  
- **Выйти со всех устройств:**
  - Инвалидирует все refresh tokens
  - Пользователь остается залогиненным только на текущем устройстве

#### 3.6.3. Региональные настройки
- Формат даты: DD.MM.YYYY | MM/DD/YYYY | YYYY-MM-DD
- Разделитель тысяч: пробел | запятая | точка
- Десятичный разделитель: точка | запятая

#### 3.6.4. Уведомления (интерфейс для будущего, без реализации в MVP)
- Email уведомления (checkbox, пока неактивен)
- Push уведомления (checkbox, пока неактивен)

---

## 4. Нефункциональные требования MVP

### 4.1. Производительность

- **Время загрузки дашборда:** < 2 секунд
- **Время расчета прогноза:** < 1 секунды для периода 36 месяцев
- **Время отклика API:** медиана < 200ms, 95 перцентиль < 500ms

### 4.2. Мультивалютность (упрощенная версия для MVP)

#### 4.2.1. Хранение данных
- Все суммы хранятся в оригинальной валюте счета
- Поле `currency` обязательно для всех счетов и операций

#### 4.2.2. Конвертация для отчетов
- Все значения на дашборде конвертируются в основную валюту пользователя
- Для исторических данных: используется курс на дату операции/reconciliation
- Для прогноза: используется текущий курс (константа на весь период)

#### 4.2.3. Источник курсов валют
- **API:** exchangerate-api.com (бесплатный tier: 1500 запросов/месяц)
- **Fallback:** api.exchangerate.host (если первый недоступен)
- **Кеширование:** раз в 24 часа (обновление в 00:00 UTC)
- **Хранение:** таблица `exchange_rates` в БД

#### 4.2.4. Поддерживаемые валюты (топ-20)
```
USD, EUR, GBP, JPY, CNY, RUB, 
AUD, CAD, CHF, SEK, NOK, DKK,
PLN, CZK, HUF, TRY, INR, BRL,
MXN, ARS
```

**Расширение списка:** в v1.1 через admin интерфейс

### 4.3. Адаптивный дизайн (Responsive Design)

#### 4.3.1. Поддерживаемые разрешения
- **Desktop:** ≥ 1200px (оптимально)
- **Tablet:** 768px - 1199px
- **Mobile:** 320px - 767px (минимум 320px)

#### 4.3.2. Брейкпоинты (ng-zorro)
```typescript
nzXs: 0-575px    // Extra small (мобильные)
nzSm: 576-767px  // Small (большие мобильные)
nzMd: 768-991px  // Medium (планшеты)
nzLg: 992-1199px // Large (десктопы)
nzXl: ≥1200px    // Extra large (большие экраны)
```

#### 4.3.3. Адаптация компонентов

**Dashboard на мобильных:**
- График: full width, высота 250px
- Карточки: вертикальный стек (по 1 на строку)
- Список событий: компактный вид (иконка + название + сумма)

**Планировщик на мобильных:**
- Режим календаря → режим списка
- Кнопка "Добавить" → floating action button (внизу справа)

**Таблицы на мобильных:**
- Горизонтальный скролл с закрепленным первым столбцом
- Или: карточный вид вместо таблицы

### 4.4. Безопасность

#### 4.4.1. HTTPS
- Обязательно в production
- Сертификаты: Let's Encrypt (автоматическое обновление)

#### 4.4.2. CORS
```python
# Настройки для production
allow_origins = ["https://финплан.example.com"]
allow_credentials = True
allow_methods = ["GET", "POST", "PUT", "DELETE"]
allow_headers = ["Content-Type", "Authorization"]
```

#### 4.4.3. Rate Limiting
- **Аутентификация:** 5 попыток в минуту на IP
- **API endpoints:** 100 запросов в минуту на пользователя
- **Публичные endpoints:** 20 запросов в минуту на IP

#### 4.4.4. Валидация данных
- Pydantic модели на backend для всех входящих данных
- XSS защита: санитизация всех строковых полей
- SQL Injection: защита через ORM (SQLAlchemy)

### 4.5. Доступность (Accessibility)

- **Уровень соответствия:** WCAG 2.1 Level AA (цель)
- **Клавиатурная навигация:** все интерактивные элементы доступны через Tab
- **Screen readers:** семантические HTML теги + ARIA атрибуты
- **Контрастность:** минимум 4.5:1 для текста
- **Масштабирование:** до 200% без потери функциональности

### 4.6. Браузеры

**Поддерживаемые:**
- Chrome/Edge: последние 2 версии
- Firefox: последние 2 версии
- Safari: последние 2 версии
- Mobile Safari (iOS): последние 2 версии
- Chrome Mobile (Android): последние 2 версии

**Не поддерживаются:** Internet Explorer

---

## 5. Структура базы данных

### 5.1. Основные таблицы

```sql
-- Пользователи
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    email VARCHAR(255) UNIQUE NOT NULL,
    hashed_password VARCHAR(255) NOT NULL,
    name VARCHAR(255),
    base_currency VARCHAR(3) NOT NULL DEFAULT 'USD',
    date_format VARCHAR(20) DEFAULT 'DD.MM.YYYY',
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP NOT NULL DEFAULT NOW()
);

-- Счета
CREATE TABLE accounts (
    id SERIAL PRIMARY KEY,
    user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    name VARCHAR(255) NOT NULL,
    type VARCHAR(50) NOT NULL, -- checking, savings, investment, credit_card, etc.
    currency VARCHAR(3) NOT NULL,
    initial_balance DECIMAL(15, 2) NOT NULL,
    initial_balance_date DATE NOT NULL,
    credit_limit DECIMAL(15, 2), -- Для кредитных счетов
    is_active BOOLEAN NOT NULL DEFAULT TRUE,
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP NOT NULL DEFAULT NOW(),
    
    CONSTRAINT check_account_type CHECK (type IN (
        'checking', 'savings', 'cash', 
        'investment', 'retirement',
        'credit_card', 'loan', 'loan_given'
    ))
);

-- Категории
CREATE TABLE categories (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id) ON DELETE CASCADE, -- NULL = системная категория
    name VARCHAR(255) NOT NULL,
    type VARCHAR(50) NOT NULL, -- income, expense, transfer
    icon VARCHAR(50), -- Название иконки из библиотеки
    color VARCHAR(7), -- Hex цвет для UI
    is_system BOOLEAN NOT NULL DEFAULT FALSE,
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    
    CONSTRAINT check_category_type CHECK (type IN ('income', 'expense', 'transfer'))
);

-- Запланированные операции
CREATE TABLE scheduled_transactions (
    id SERIAL PRIMARY KEY,
    user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    account_id INTEGER NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
    to_account_id INTEGER REFERENCES accounts(id) ON DELETE CASCADE, -- Для переводов
    category_id INTEGER NOT NULL REFERENCES categories(id),
    
    name VARCHAR(255) NOT NULL,
    amount DECIMAL(15, 2) NOT NULL,
    currency VARCHAR(3) NOT NULL,
    note TEXT,
    
    -- Recurring параметры
    is_recurring BOOLEAN NOT NULL DEFAULT FALSE,
    recurrence_frequency VARCHAR(20), -- 'MONTHLY', 'YEARLY'
    recurrence_day_of_month INTEGER, -- 1-31 или -1 для последнего дня
    recurrence_month_of_year INTEGER, -- 1-12, только для YEARLY
    recurrence_start_date DATE NOT NULL,
    recurrence_end_date DATE,
    
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP NOT NULL DEFAULT NOW(),
    
    CONSTRAINT check_recurrence_frequency CHECK (
        recurrence_frequency IS NULL OR 
        recurrence_frequency IN ('MONTHLY', 'YEARLY')
    ),
    CONSTRAINT check_day_of_month CHECK (
        recurrence_day_of_month IS NULL OR
        (recurrence_day_of_month BETWEEN -1 AND 31)
    ),
    CONSTRAINT check_month_of_year CHECK (
        recurrence_month_of_year IS NULL OR
        (recurrence_month_of_year BETWEEN 1 AND 12)
    )
);

-- Исключения из recurring серий
CREATE TABLE scheduled_transaction_exceptions (
    id SERIAL PRIMARY KEY,
    scheduled_transaction_id INTEGER NOT NULL REFERENCES scheduled_transactions(id) ON DELETE CASCADE,
    exception_date DATE NOT NULL,
    
    -- Измененные значения (NULL = используется из scheduled_transaction)
    amount DECIMAL(15, 2),
    note TEXT,
    
    -- Флаг удаления (пропустить это вхождение)
    is_deleted BOOLEAN NOT NULL DEFAULT FALSE,
    
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    
    UNIQUE(scheduled_transaction_id, exception_date)
);

-- История корректировок баланса
CREATE TABLE account_reconciliations (
    id SERIAL PRIMARY KEY,
    account_id INTEGER NOT NULL REFERENCES accounts(id) ON DELETE CASCADE,
    reconciliation_date DATE NOT NULL,
    expected_balance DECIMAL(15, 2) NOT NULL, -- Что было по прогнозу
    actual_balance DECIMAL(15, 2) NOT NULL,   -- Что на самом деле
    adjustment_amount DECIMAL(15, 2) NOT NULL, -- Разница
    note TEXT,
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    
    UNIQUE(account_id, reconciliation_date)
);

-- Курсы валют (кеш)
CREATE TABLE exchange_rates (
    id SERIAL PRIMARY KEY,
    from_currency VARCHAR(3) NOT NULL,
    to_currency VARCHAR(3) NOT NULL,
    rate DECIMAL(18, 8) NOT NULL,
    date DATE NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    
    UNIQUE(from_currency, to_currency, date)
);

-- Refresh tokens для JWT
CREATE TABLE refresh_tokens (
    id SERIAL PRIMARY KEY,
    user_id INTEGER NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    token VARCHAR(500) NOT NULL UNIQUE,
    expires_at TIMESTAMP NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    is_revoked BOOLEAN NOT NULL DEFAULT FALSE
);

-- Индексы для производительности
CREATE INDEX idx_accounts_user_id ON accounts(user_id);
CREATE INDEX idx_scheduled_transactions_user_id ON scheduled_transactions(user_id);
CREATE INDEX idx_scheduled_transactions_account_id ON scheduled_transactions(account_id);
CREATE INDEX idx_reconciliations_account_id ON account_reconciliations(account_id);
CREATE INDEX idx_reconciliations_date ON account_reconciliations(reconciliation_date);
CREATE INDEX idx_exchange_rates_lookup ON exchange_rates(from_currency, to_currency, date);
CREATE INDEX idx_refresh_tokens_user_id ON refresh_tokens(user_id);
CREATE INDEX idx_refresh_tokens_token ON refresh_tokens(token);
```

### 5.2. Начальные данные (Seed Data)

#### Системные категории:

```sql
-- Доходы
INSERT INTO categories (name, type, icon, color, is_system) VALUES
('Зарплата', 'income', 'wallet', '#52c41a', TRUE),
('Фриланс', 'income', 'code', '#52c41a', TRUE),
('Инвестиционный доход', 'income', 'stock', '#52c41a', TRUE),
('Продажа имущества', 'income', 'home', '#52c41a', TRUE),
('Прочий доход', 'income', 'plus-circle', '#52c41a', TRUE);

-- Расходы
INSERT INTO categories (name, type, icon, color, is_system) VALUES
('Базовые расходы', 'expense', 'shopping-cart', '#f5222d', TRUE),
('Аренда / Ипотека', 'expense', 'home', '#f5222d', TRUE),
('Коммунальные услуги', 'expense', 'thunderbolt', '#f5222d', TRUE),
('Страхование', 'expense', 'safety', '#f5222d', TRUE),
('Образование', 'expense', 'book', '#f5222d', TRUE),
('Здоровье', 'expense', 'medicine-box', '#f5222d', TRUE),
('Крупная покупка', 'expense', 'shopping', '#f5222d', TRUE),
('Прочие расходы', 'expense', 'minus-circle', '#f5222d', TRUE);

-- Переводы
INSERT INTO categories (name, type, icon, color, is_system) VALUES
('В инвестиции', 'transfer', 'rise', '#1890ff', TRUE),
('Между счетами', 'transfer', 'swap', '#1890ff', TRUE),
('Погашение кредита', 'transfer', 'credit-card', '#1890ff', TRUE);
```

---

## 6. API Спецификация (основные endpoints)

### 6.1. Аутентификация

```
POST /api/v1/auth/register
Request:  { email, password }
Response: { user: { id, email, base_currency }, access_token, refresh_token }

POST /api/v1/auth/login
Request:  { email, password }
Response: { user: { id, email, name, base_currency }, access_token, refresh_token }

POST /api/v1/auth/refresh
Request:  { refresh_token }
Response: { access_token, refresh_token }

POST /api/v1/auth/logout
Headers:  Authorization: Bearer <token>
Response: { message: "Logged out successfully" }

POST /api/v1/auth/logout-all
Headers:  Authorization: Bearer <token>
Response: { message: "Logged out from all devices" }
```

### 6.2. Дашборд

```
GET /api/v1/dashboard
Headers: Authorization: Bearer <token>
Query:   ?from=2024-11-01&to=2026-12-31
Response: {
  summary: {
    liquidAssets: 12450.00,
    investments: 35000.00,
    creditUsed: 1500.00,
    creditAvailable: 3500.00,
    loansReceivable: 500.00,
    netWorth: 46450.00
  },
  forecast: [
    {
      date: "2024-11-01",
      liquidAssets: 12000.00,
      investments: 34000.00,
      creditUsed: 1200.00,
      loansReceivable: 500.00
    },
    ...
  ],
  upcomingEvents: [
    {
      id: 123,
      date: "2025-11-25",
      name: "Зарплата",
      amount: 5000.00,
      type: "income",
      category: "Зарплата"
    },
    ...
  ],
  alerts: [
    {
      type: "reconciliation_needed",
      message: "Баланс карты Сбер отличается от прогноза на $230",
      accountId: 5,
      actionUrl: "/accounts/5/reconcile"
    }
  ]
}
```

### 6.3. Счета

```
GET /api/v1/accounts
Headers:  Authorization: Bearer <token>
Response: {
  accounts: [
    {
      id: 1,
      name: "Дебетовая карта Сбер",
      type: "checking",
      currency: "RUB",
      currentBalance: 125000.00,
      creditLimit: null,
      isActive: true
    },
    ...
  ]
}

POST /api/v1/accounts
Headers: Authorization: Bearer <token>
Request: {
  name: "Новый счет",
  type: "checking",
  currency: "USD",
  initialBalance: 1000.00,
  initialBalanceDate: "2025-11-01",
  creditLimit: null
}
Response: { account: {...} }

PUT /api/v1/accounts/{id}
Headers: Authorization: Bearer <token>
Request: { name: "Обновленное имя", creditLimit: 5000.00 }
Response: { account: {...} }

DELETE /api/v1/accounts/{id}
Headers: Authorization: Bearer <token>
Response: { message: "Account deleted successfully" }

POST /api/v1/accounts/{id}/reconcile
Headers: Authorization: Bearer <token>
Request: {
  reconciliationDate: "2025-11-08",
  actualBalance: 12680.00,
  note: "Забыл внести покупку ноутбука"
}
Response: {
  reconciliation: {
    id: 10,
    expectedBalance: 12450.00,
    actualBalance: 12680.00,
    adjustmentAmount: 230.00
  }
}
```

### 6.4. Запланированные операции

```
GET /api/v1/scheduled-transactions
Headers:  Authorization: Bearer <token>
Response: {
  transactions: [
    {
      id: 1,
      name: "Зарплата",
      amount: 5000.00,
      currency: "USD",
      account: { id: 1, name: "Checking" },
      category: { id: 1, name: "Зарплата" },
      isRecurring: true,
      recurrence: {
        frequency: "MONTHLY",
        dayOfMonth: 25,
        startDate: "2024-01-01",
        endDate: null
      }
    },
    ...
  ]
}

GET /api/v1/scheduled-transactions/instances
Headers: Authorization: Bearer <token>
Query:   ?from=2025-11-01&to=2025-12-31
Response: {
  instances: [
    {
      date: "2025-11-25",
      scheduledTransactionId: 1,
      name: "Зарплата",
      amount: 5000.00,
      category: "Зарплата",
      isException: false
    },
    ...
  ]
}

POST /api/v1/scheduled-transactions
Headers: Authorization: Bearer <token>
Request: {
  name: "Покупка MacBook",
  amount: 2500.00,
  currency: "USD",
  accountId: 1,
  categoryId: 7,
  note: "MacBook Pro 14",
  isRecurring: false,
  recurrenceStartDate: "2025-12-15"
}
Response: { transaction: {...} }

PUT /api/v1/scheduled-transactions/{id}
Headers: Authorization: Bearer <token>
Query:   ?updateMode=THIS_ONLY|ALL|THIS_AND_FUTURE&instanceDate=2025-12-25
Request: { amount: 6500.00 }
Response: { transaction: {...} }

DELETE /api/v1/scheduled-transactions/{id}
Headers: Authorization: Bearer <token>
Query:   ?deleteMode=THIS_ONLY|ALL|THIS_AND_FUTURE&instanceDate=2025-12-25
Response: { message: "Transaction deleted successfully" }
```

### 6.5. Прогноз

```
GET /api/v1/forecast
Headers: Authorization: Bearer <token>
Query:   ?from=2024-11-01&to=2026-11-30&accountTypes=checking,savings&granularity=day
Response: {
  dataPoints: [
    {
      date: "2024-11-01",
      liquidAssets: 12000.00,
      investments: 34000.00,
      creditUsed: 1200.00,
      loansReceivable: 500.00,
      breakdown: {
        byAccount: [
          { accountId: 1, accountName: "Сбер", balance: 8000.00 },
          ...
        ],
        byCategory: [
          { categoryId: 1, categoryName: "Зарплата", amount: 5000.00 },
          ...
        ]
      }
    },
    ...
  ]
}
```

### 6.6. Анализ

```
GET /api/v1/analysis
Headers: Authorization: Bearer <token>
Query:   ?from=2025-08-01&to=2025-10-31
Response: {
  summary: {
    planned: {
      income: 15000.00,
      expenses: 7500.00,
      savings: 7500.00
    },
    actual: {
      income: 15800.00,
      expenses: 8200.00,
      savings: 7600.00
    },
    deviation: {
      income: { amount: 800.00, percentage: 5.3 },
      expenses: { amount: 700.00, percentage: 9.3 },
      savings: { amount: 100.00, percentage: 1.3 }
    }
  },
  byCategory: [
    {
      categoryId: 1,
      categoryName: "Зарплата",
      type: "income",
      planned: 15000.00,
      actual: 15800.00,
      deviation: { amount: 800.00, percentage: 5.3 }
    },
    ...
  ]
}
```

### 6.7. Категории

```
GET /api/v1/categories
Headers: Authorization: Bearer <token>
Response: {
  categories: [
    {
      id: 1,
      name: "Зарплата",
      type: "income",
      icon: "wallet",
      color: "#52c41a",
      isSystem: true
    },
    ...
  ]
}

POST /api/v1/categories
Headers: Authorization: Bearer <token>
Request: {
  name: "Моя категория",
  type: "expense",
  icon: "custom-icon",
  color: "#ff0000"
}
Response: { category: {...} }
```

### 6.8. Настройки

```
GET /api/v1/settings
Headers:  Authorization: Bearer <token>
Response: {
  user: {
    id: 1,
    email: "user@example.com",
    name: "Иван Иванов",
    baseCurrency: "USD",
    dateFormat: "DD.MM.YYYY"
  }
}

PUT /api/v1/settings
Headers: Authorization: Bearer <token>
Request: {
  name: "Новое имя",
  baseCurrency: "EUR",
  dateFormat: "DD/MM/YYYY"
}
Response: { user: {...} }

PUT /api/v1/settings/password
Headers: Authorization: Bearer <token>
Request: {
  currentPassword: "old123",
  newPassword: "new456"
}
Response: { message: "Password updated successfully" }
```

---

## 7. Структура frontend приложения

### 7.1. Модули (Feature Modules)

```
src/
├── app/
│   ├── core/                    # Singleton сервисы
│   │   ├── services/
│   │   │   ├── auth.service.ts
│   │   │   ├── api.service.ts
│   │   │   └── storage.service.ts
│   │   ├── guards/
│   │   │   └── auth.guard.ts
│   │   ├── interceptors/
│   │   │   ├── auth.interceptor.ts
│   │   │   └── error.interceptor.ts
│   │   └── core.module.ts
│   │
│   ├── shared/                  # Переиспользуемые компоненты
│   │   ├── components/
│   │   │   ├── page-header/
│   │   │   ├── loading-spinner/
│   │   │   └── empty-state/
│   │   ├── pipes/
│   │   │   ├── currency-format.pipe.ts
│   │   │   └── date-format.pipe.ts
│   │   └── shared.module.ts
│   │
│   ├── features/
│   │   ├── auth/
│   │   │   ├── pages/
│   │   │   │   ├── login/
│   │   │   │   └── register/
│   │   │   ├── auth.service.ts
│   │   │   ├── auth.module.ts
│   │   │   └── auth-routing.module.ts
│   │   │
│   │   ├── dashboard/
│   │   │   ├── components/
│   │   │   │   ├── forecast-chart/
│   │   │   │   ├── summary-cards/
│   │   │   │   ├── upcoming-events/
│   │   │   │   └── alerts-block/
│   │   │   ├── pages/
│   │   │   │   └── dashboard/
│   │   │   ├── dashboard.service.ts
│   │   │   ├── dashboard.module.ts
│   │   │   └── dashboard-routing.module.ts
│   │   │
│   │   ├── accounts/
│   │   │   ├── components/
│   │   │   │   ├── account-list/
│   │   │   │   ├── account-form/
│   │   │   │   └── reconciliation-modal/
│   │   │   ├── pages/
│   │   │   │   ├── accounts/
│   │   │   │   └── account-detail/
│   │   │   ├── accounts.service.ts
│   │   │   ├── accounts.module.ts
│   │   │   └── accounts-routing.module.ts
│   │   │
│   │   ├── scheduler/
│   │   │   ├── components/
│   │   │   │   ├── transaction-list/
│   │   │   │   ├── transaction-form/
│   │   │   │   ├── recurrence-config/
│   │   │   │   └── edit-recurring-modal/
│   │   │   ├── pages/
│   │   │   │   └── scheduler/
│   │   │   ├── scheduler.service.ts
│   │   │   ├── scheduler.module.ts
│   │   │   └── scheduler-routing.module.ts
│   │   │
│   │   ├── analysis/
│   │   │   ├── components/
│   │   │   │   ├── plan-vs-actual-chart/
│   │   │   │   └── category-breakdown/
│   │   │   ├── pages/
│   │   │   │   └── analysis/
│   │   │   ├── analysis.service.ts
│   │   │   ├── analysis.module.ts
│   │   │   └── analysis-routing.module.ts
│   │   │
│   │   └── settings/
│   │       ├── pages/
│   │       │   └── settings/
│   │       ├── settings.service.ts
│   │       ├── settings.module.ts
│   │       └── settings-routing.module.ts
│   │
│   ├── app.component.ts
│   ├── app.module.ts
│   └── app-routing.module.ts
│
├── assets/
│   ├── images/
│   └── i18n/              # Для будущей локализации
│
└── environments/
    ├── environment.ts
    └── environment.prod.ts
```

### 7.2. Роутинг

```typescript
const routes: Routes = [
  { path: '', redirectTo: '/dashboard', pathMatch: 'full' },
  {
    path: 'auth',
    loadChildren: () => import('./features/auth/auth.module').then(m => m.AuthModule)
  },
  {
    path: 'dashboard',
    loadChildren: () => import('./features/dashboard/dashboard.module').then(m => m.DashboardModule),
    canActivate: [AuthGuard]
  },
  {
    path: 'accounts',
    loadChildren: () => import('./features/accounts/accounts.module').then(m => m.AccountsModule),
    canActivate: [AuthGuard]
  },
  {
    path: 'scheduler',
    loadChildren: () => import('./features/scheduler/scheduler.module').then(m => m.SchedulerModule),
    canActivate: [AuthGuard]
  },
  {
    path: 'analysis',
    loadChildren: () => import('./features/analysis/analysis.module').then(m => m.AnalysisModule),
    canActivate: [AuthGuard]
  },
  {
    path: 'settings',
    loadChildren: () => import('./features/settings/settings.module').then(m => m.SettingsModule),
    canActivate: [AuthGuard]
  },
  { path: '**', redirectTo: '/dashboard' }
];
```

---

## 8. План разработки MVP

### 8.1. Этап 1: Инфраструктура и базовая настройка (Неделя 1)

**Backend:**
- [x] Настройка проекта: uv init, структура папок
- [x] FastAPI базовое приложение + CORS
- [x] Подключение PostgreSQL (локально через Docker)
- [x] Настройка Alembic для миграций
- [x] Базовые модели SQLAlchemy: User, Account
- [x] JWT аутентификация (login, register)
- [x] Middleware для обработки ошибок

**Frontend:**
- [x] Создание Angular проекта
- [x] Настройка ng-zorro-antd
- [x] Базовая структура модулей
- [x] AuthGuard, AuthInterceptor
- [x] Страницы Login и Register

**DevOps:**
- [x] Docker Compose для локальной разработки
- [x] .env файлы для конфигурации

### 8.2. Этап 2: Счета и базовый дашборд (Неделя 2)

**Backend:**
- [x] CRUD API для счетов
- [x] Модель Category + seed данных
- [x] Эндпоинт для получения summary (без прогноза)
- [x] Базовая таблица exchange_rates + API для курсов

**Frontend:**
- [x] Модуль Accounts: список, форма создания/редактирования
- [x] Dashboard: карточки summary
- [x] Shared компоненты: page-header, empty-state

### 8.3. Этап 3: Планировщик операций (Неделя 3-4)

**Backend:**
- [x] Модель ScheduledTransaction
- [x] CRUD API для scheduled transactions
- [x] Модель ScheduledTransactionException
- [x] Логика expand_recurring_transactions
- [x] API для получения instances на период

**Frontend:**
- [x] Модуль Scheduler: список recurring операций
- [x] Форма создания операции (разовая + recurring)
- [x] Модальное окно редактирования recurring операций
- [x] Календарь развернутых событий (опционально)

### 8.4. Этап 4: Прогноз (Неделя 5)

**Backend:**
- [x] Сервис ForecastService: расчет прогноза
- [x] API эндпоинт /forecast
- [x] Оптимизация запросов (кеширование курсов)

**Frontend:**
- [x] Dashboard: график прогноза (ECharts)
- [x] Фильтры периода прогноза
- [x] Список ближайших событий

### 8.5. Этап 5: Reconciliation и анализ (Неделя 6)

**Backend:**
- [x] Модель AccountReconciliation
- [x] API для reconciliation
- [x] API для анализа (план vs факт)

**Frontend:**
- [x] Модальное окно reconciliation
- [x] Блок "Требуется внимание" на дашборде
- [x] Страница анализа (упрощенная версия)

### 8.6. Этап 6: Настройки и полировка (Неделя 7)

**Backend:**
- [x] API для настроек пользователя
- [x] API для смены пароля
- [x] Рефакторинг + оптимизация запросов

**Frontend:**
- [x] Страница настроек
- [x] Улучшение UX (loading states, error messages)
- [x] Адаптация для мобильных устройств

### 8.7. Этап 7: Тестирование и деплой (Неделя 8-9)

**Backend:**
- [x] Unit тесты для ForecastService
- [x] Integration тесты для API
- [x] Подготовка к деплою (environment variables)

**Frontend:**
- [x] E2E тесты основных сценариев
- [x] Тестирование на разных браузерах
- [x] Оптимизация сборки (lazy loading, tree shaking)

**DevOps:**
- [x] CI/CD pipeline (GitHub Actions или GitLab CI)
- [x] Деплой на Railway/Render/Fly.io
- [x] Настройка domain + SSL

---

## 9. Планы на версию v1.1

### 9.1. Функциональные улучшения

#### 9.1.1. Расширенные правила повторения
- **Еженедельные операции:** "Каждый понедельник", "По вторникам и четвергам"
- **Ежедневные операции:** для детального планирования
- **Сложные паттерны:** "Каждый второй вторник месяца", "Каждые 3 месяца"
- **Стандарт iCalendar (RFC 5545):** полная поддержка RRULE

#### 9.1.2. История параметров счетов
- Таблица `account_parameter_history`
- Отслеживание изменений:
  - Кредитного лимита
  - Процентной ставки (для будущего)
  - Названия счета
- UI для просмотра истории изменений
- Прогноз с учетом исторических параметров

#### 9.1.3. Сценарии "Что если"
- Создание альтернативных прогнозов
- Возможность добавить временные операции без влияния на основной план
- Сравнение сценариев (side-by-side)
- Примеры сценариев:
  - "Что если куплю машину в марте vs июне?"
  - "Что если перейду на другую работу с зарплатой на 20% ниже?"

#### 9.1.4. Детальный анализ прошлого
- Круговые диаграммы по подкатегориям
- Тренды расходов/доходов по месяцам
- Сравнение периодов (месяц к месяцу, год к году)
- Выявление аномалий ("В этом месяце потратили на 40% больше обычного")

#### 9.1.5. Кастомные категории
- Пользователь может создавать свои категории
- Подкатегории (иерархия)
- Привязка категорий к целям (goals)

#### 9.1.6. Цели и накопления (Goals)
- Создание целей: "Накопить на машину $25,000 к августу 2026"
- Трекинг прогресса на дашборде
- Автоматические рекомендации: "Для достижения цели откладывайте $800/месяц"
- Связь целей с операциями

#### 9.1.7. Теги для операций
- Возможность добавить несколько тегов к операции
- Фильтрация и анализ по тегам
- Примеры тегов: #отпуск, #ремонт, #дети, #бизнес

#### 9.1.8. Совместный доступ (Shared accounts)
- Приглашение других пользователей для просмотра/редактирования
- Роли: владелец, редактор, наблюдатель
- Семейный бюджет (несколько пользователей на одном аккаунте)

### 9.2. Технические улучшения

#### 9.2.1. Email верификация и восстановление пароля
- Отправка email с кодом подтверждения при регистрации
- Функционал "Забыли пароль?"
- Интеграция с email сервисом (SendGrid, AWS SES)

#### 9.2.2. State management на frontend
- Внедрение NgRx (Redux pattern)
- Оптимизация re-renders
- Offline-first подход (кеширование данных)

#### 9.2.3. Уведомления
- **Push notifications:**
  - Напоминания о предстоящих платежах
  - Уведомления о превышении бюджета
  - Alerts о необходимости reconciliation
  
- **Email уведомления:**
  - Еженедельный/ежемесячный дайджест
  - Критичные события (баланс уходит в минус)

#### 9.2.4. Экспорт и импорт данных
- **Экспорт:**
  - CSV (для Excel)
  - PDF (финансовый отчет)
  - JSON (полный backup)
  
- **Импорт:**
  - CSV выписки из банка (с маппингом на категории)
  - JSON backup

#### 9.2.5. Интеграция с банками
- Open Banking API (для стран ЕС)
- Plaid API (для США)
- Tinkoff API (для России)
- Автоматическая синхронизация балансов

#### 9.2.6. Мобильное приложение
- React Native или Flutter
- Нативные push уведомления
- Быстрый просмотр баланса через виджет
- Face ID / Touch ID для входа

#### 9.2.7. Производительность и масштабирование
- Кеширование с Redis
- Background jobs для расчета прогнозов (Celery)
- Pagination для больших списков
- Оптимизация SQL запросов (N+1 проблема)

#### 9.2.8. Безопасность
- Two-factor authentication (2FA)
- Аудит лог всех действий пользователя
- Rate limiting на уровне пользователя
- Шифрование чувствительных данных

#### 9.2.9. Аналитика и мониторинг
- Google Analytics / Mixpanel (user behavior)
- Sentry (error tracking)
- Grafana + Prometheus (метрики сервера)
- Heatmaps (Hotjar)

### 9.3. UX улучшения

#### 9.3.1. Onboarding
- Welcome tour для новых пользователей
- Пошаговая настройка:
  1. Добавить первый счет
  2. Создать первую recurring операцию (зарплата)
  3. Добавить базовые расходы
  4. Посмотреть прогноз
- Tooltips на первых запусках

#### 9.3.2. Темная тема
- Dark mode для комфортного использования вечером
- Автоматическое переключение по времени суток

#### 9.3.3. Локализация (i18n)
- Английский язык (приоритет)
- Испанский
- Немецкий
- Французский
- Адаптация валют по локали

#### 9.3.4. Keyboard shortcuts
- `Ctrl/Cmd + K` — быстрый поиск
- `A` — добавить операцию
- `R` — reconciliation
- `D` — перейти на дашборд

#### 9.3.5. Улучшения графиков
- Режим сравнения (overlay нескольких периодов)
- Аннотации на графике (отметить важные события)
- Экспорт графика как изображение
- Интерактивные фильтры (выбор счетов, категорий)

### 9.4. Бизнес-функционал

#### 9.4.1. Монетизация
- **Freemium модель:**
  - Free tier: до 3 счетов, до 10 recurring операций
  - Pro tier ($9.99/мес): безлимит + расширенная аналитика
  - Family tier ($14.99/мес): совместный доступ до 5 пользователей
  
- **Страница подписки:**
  - Stripe integration
  - Управление подпиской
  - История платежей

#### 9.4.2. Рефералки
- Реферальная программа: "Пригласи друга — получи месяц Pro бесплатно"
- Уникальная реферальная ссылка для каждого пользователя
- Трекинг конверсий

#### 9.4.3. Интеграция с финансовыми советниками
- API для финансовых консультантов
- Возможность давать доступ своему советнику
- Комментарии и рекомендации от советника в приложении

### 9.5. AI и автоматизация

#### 9.5.1. Умные рекомендации
- "У вас регулярные траты ~$150 каждое 15 число. Создать recurring операцию?"
- "Ваши базовые расходы выросли на 15% за последние 3 месяца. Пересмотреть бюджет?"
- "На основе текущих трендов вы достигнете цели на 2 месяца раньше"

#### 9.5.2. Категоризация транзакций через ML
- Автоматическое определение категории при импорте из банка
- Обучение на данных пользователя
- Ручная корректировка → улучшение модели

#### 9.5.3. Прогнозирование на основе истории
- Предсказание нерегулярных расходов ("Обычно в декабре тратите на 30% больше")
- Сезонные корректировки прогноза

---

## 10. Потенциальные риски и митигации

### 10.1. Технические риски

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Производительность расчета прогноза при большом количестве операций | Средняя | Высокое | - Оптимизация SQL запросов<br>- Кеширование результатов<br>- Pagination<br>- Background jobs |
| Проблемы с API курсов валют (блокировка, лимиты) | Высокая | Среднее | - Несколько fallback API<br>- Кеширование на 24 часа<br>- Graceful degradation |
| Сложность логики "split" recurring операций | Средняя | Высокое | - Тщательное проектирование<br>- Unit тесты всех edge cases<br>- Упрощение в MVP |
| Безопасность (утечка финансовых данных) | Низкая | Критическое | - Encryption at rest<br>- HTTPS only<br>- Regular security audits<br>- Compliance с GDPR |

### 10.2. Бизнес-риски

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Низкая retention (пользователи не возвращаются) | Высокая | Критическое | - Push notifications<br>- Email reminders<br>- Gamification<br>- Clear value proposition |
| Сложность onboarding (пользователи не понимают как использовать) | Средняя | Высокое | - Welcome tour<br>- Tooltips<br>- Video tutorials<br>- Templates для быстрого старта |
| Конкуренция с Excel/Google Sheets | Высокая | Среднее | - Фокус на UX<br>- Автоматизация<br>- Визуализация<br>- Mobile-first |
| Недостаточная монетизация | Средняя | Высокое | - A/B тестирование цен<br>- Четкий value для Pro tier<br>- Lifetime deals на старте |

### 10.3. User experience риски

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Пользователи ожидают автоматический импорт из банка | Высокая | Среднее | - Четко коммуницировать философию "макро-планирования"<br>- Добавить импорт в v1.1<br>- CSV импорт как промежуточное решение |
| Недостаточная гибкость категорий | Средняя | Среднее | - Кастомные категории в v1.1<br>- Теги как альтернатива<br>- Хорошие дефолтные категории |
| Reconciliation слишком сложен для понимания | Средняя | Высокое | - Простой UI<br>- Tooltips с объяснением<br>- Видео-туториал<br>- Auto-reconciliation предложения |

---

## 11. Метрики успеха

### 11.1. Product metrics (3 месяца после запуска)

- **Activation rate:** > 70% (пользователь создал хотя бы 1 счет и 1 операцию)
- **D7 retention:** > 40%
- **D30 retention:** > 25%
- **MAU/DAU ratio:** ~0.15 (пользователи заходят 4-5 раз в месяц)
- **Average session duration:** 5-7 минут
- **Feature adoption:**
  - Recurring операции: > 80%
  - Reconciliation: > 60%
  - Единовременные события: > 70%
  - Анализ: > 40%

### 11.2. Business metrics (6 месяцев после запуска)

- **Total users:** 1,000+
- **Paying users:** 100+ (10% conversion rate)
- **MRR (Monthly Recurring Revenue):** $999+
- **Churn rate:** < 5% в месяц
- **LTV/CAC ratio:** > 3:1

### 11.3. Technical metrics

- **API response time (p95):** < 500ms
- **Uptime:** > 99.5%
- **Error rate:** < 1%
- **Forecast calculation time:** < 1s для 36 месяцев

---

## 12. Заключение

Данный документ описывает минимально жизнеспособный продукт (MVP) финансового планировщика с фокусом на среднесрочное и долгосрочное планирование.

**Ключевые особенности MVP:**
- ✅ Простой и быстрый онбординг (< 10 минут до первого прогноза)
- ✅ Фокус на крупных финансовых событиях, а не микротранзакциях
- ✅ Визуализация траектории капитала в 4 категориях
- ✅ Reconciliation для синхронизации с реальностью
- ✅ Гибкие recurring операции (MONTHLY и YEARLY)
- ✅ Responsive дизайн для мобильных устройств

**Упрощения для MVP:**
- 🟡 Только два типа повторений (ежемесячно и ежегодно)
- 🟡 Упрощенный модуль анализа (план vs факт)
- 🟡 Без истории параметров счетов
- 🟡 Без email верификации
- 🟡 Без интеграции с банками

**Планы на v1.1:**
- 📅 Расширенные правила повторений (еженедельно, ежедневно, сложные паттерны)
- 📊 Детальный анализ с подкатегориями и трендами
- 🎯 Цели и накопления (goals)
- 👥 Совместный доступ (family budgeting)
- 📧 Email уведомления
- 🏦 Интеграция с банками
- 📱 Мобильное приложение

**Оценка времени разработки MVP:** 8-9 недель при работе вечерами и выходными

---

**Следующие шаги:**
1. ✅ Создание финального документа с требованиями (выполнено)
2. 🔄 Детальное проектирование схемы БД
3. 🔄 Прототипирование UI (Figma/Excalidraw)
4. 🔄 Настройка структуры FastAPI + Angular проектов
5. 🔄 Начало разработки с этапа 1

---

**Контакты для вопросов:** [Оставить пустым или указать email]

**Лицензия:** [Определить позже]

**Версия документа:** 1.0 (08.11.2025)
